<h1>New Style</h1>


<%= form_for(@style) do |f| %>
  <% if @style.errors.any? %>
    <h2><%= pluralize(@style.errors.count, "error") %> prohibited this record from being saved:</h2>
    <ul>
      <% @style.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit %>
<% end %>
